<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.hebin.mduse">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".ui.activity.MainActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden|adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.RecyclerDetailActivity"/>
        <activity android:name=".ui.activity.WidgetActivity"/>
        <activity android:name=".ui.activity.MDViewActivity"/>
        <activity android:name=".ui.transition.ExplodeActivity"/>
        <activity android:name=".ui.transition.FadeActivity"/>
        <activity android:name=".ui.transition.ShareActivity"/>
        <activity android:name=".ui.transition.SlideActivity"/>
        <activity android:name=".ui.transition.TransitionsActivity"/>
        <activity android:name=".ui.activity.MDViewActivity"/>
    </application>

</manifest>